# NOTE:
# APPLICATION   - Name application
# PACKAGE_PATH  - Path to package.json
.matrix_build_npm:
  parallel:
    matrix:
      - APPLICATION: eslint-plugin-shortlink
        PACKAGE_PATH: "./eslint-plugin-shortlink"
      - APPLICATION: eslint-config-shortlink
        PACKAGE_PATH: "./eslint-config-shortlink"
